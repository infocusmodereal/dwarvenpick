services:
  postgres:
    image: postgres:15-alpine
    container_name: badgermole-postgres
    environment:
      POSTGRES_USER: badgermole
      POSTGRES_PASSWORD: badgermole
      POSTGRES_DB: badgermole
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  mysql:
    image: mysql:8.4
    container_name: badgermole-mysql
    environment:
      MYSQL_ROOT_PASSWORD: badgermole
      MYSQL_DATABASE: orders
      MYSQL_USER: readonly
      MYSQL_PASSWORD: readonly
    ports:
      - "3306:3306"
    volumes:
      - mysqldata:/var/lib/mysql
      - ./mysql-init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro

  backend:
    build:
      context: ../../
      dockerfile: deploy/docker/Dockerfile.backend
    container_name: badgermole-backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/badgermole
      SPRING_DATASOURCE_USERNAME: badgermole
      SPRING_DATASOURCE_PASSWORD: badgermole
      BADGERMOLE_SEED_POSTGRES_HOST: postgres
      BADGERMOLE_SEED_POSTGRES_PORT: 5432
      BADGERMOLE_SEED_POSTGRES_DATABASE: badgermole
      BADGERMOLE_SEED_POSTGRES_USERNAME: badgermole
      BADGERMOLE_SEED_POSTGRES_PASSWORD: badgermole
      BADGERMOLE_SEED_MYSQL_HOST: mysql
      BADGERMOLE_SEED_MYSQL_PORT: 3306
      BADGERMOLE_SEED_MYSQL_DATABASE: orders
      BADGERMOLE_SEED_MYSQL_USERNAME: readonly
      BADGERMOLE_SEED_MYSQL_PASSWORD: readonly
      SERVER_PORT: 8080
    ports:
      - "8080:8080"
    depends_on:
      - postgres
      - mysql

  frontend:
    build:
      context: ../../
      dockerfile: deploy/docker/Dockerfile.frontend
    container_name: badgermole-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  pgdata:
  mysqldata:
